import { Icon } from './icon-base';

// Exercises the two skip branches in renderModule: the bare Icon wrapper has no
// derivable name, and IconLabel is named like an icon but is not a component.
export { Icon };

export const IconLabel = 'not a component';
